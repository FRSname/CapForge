# CLAUDE.md

Guidance for Claude Code (claude.ai/code) when working in this repository.

## What This Is

CapForge is a desktop subtitle editor: Electron 33 shell → React 19 renderer → Python FastAPI backend. The backend runs WhisperX for transcription and Pillow/FFmpeg for video rendering. Electron spawns the Python process on startup; the renderer talks to it over REST + WebSocket on `127.0.0.1` (preferred port 53421, falls back to an ephemeral port).

## Git Operations

Delegate all git **write** work — `commit`, `merge`, `push`, `branch`, `rebase`, conflict resolution — to the `git-ops` subagent (`Agent(subagent_type: "git-ops", …)`). It follows conventional-commit format, never commits/pushes directly to the default branch (branches first), and confirms before the first push. The read-only `scout` agent must never be used for git writes. Read-only git inspection (`status`, `diff`, `log`) can be run inline or by `scout`.

## Build & Dev Commands

```bash
npm run dev:react          # Vite dev server with HMR — THE live-reload path
npm run dev                # electron . --dev — does NOT start Vite (see trap below)
npm start                  # Production mode (needs build:react first)
npm run build:react        # Production build → out/renderer/

npm run typecheck          # tsc --noEmit -p tsconfig.web.json
npm test                   # vitest run
npm run lint               # eslint src

npm run backend            # uvicorn backend.main:app --host 127.0.0.1 --port 53421
npm run dist:mac           # DMG   (dist:* run build:react first)
npm run dist:win           # NSIS installer
```

**`npm run dev` is a trap**: it launches Electron with `--dev` but does not start Vite, so renderer edits won't appear. Use `npm run dev:react`.

### Testing

- **Frontend**: vitest, ~465 tests. Runs in the **`node` environment, not jsdom** — component tests render via `react-dom/server` to static HTML and assert on markup, so there are no DOM events and no testing of hooks-with-effects. Write new tests to that constraint.
- **Backend**: pytest. `pyproject.toml` sets `testpaths = ["backend/tests"]`, so a bare `pytest` **silently skips `mcp_server/tests`** — pass that path explicitly.
- **CI** (`.github/workflows/ci.yml`): three jobs — frontend (typecheck + vitest + lint), backend (pytest), caption-parity.
- eslint (flat config) + prettier are configured, but **`eslint.config.js` deliberately ignores `electron/**`** — `npm run lint` passing says nothing about the Electron layer.

## Architecture

### Three-Layer Stack

1. **Electron main process** (`electron/`) — vanilla JS. `main.js` creates the window, `python-manager.js` spawns/manages the backend, `runtime-setup.js` handles first-launch Python/model downloads, `preload.js` bridges IPC.
2. **React renderer** (`src/renderer/src/`) — TypeScript + React 19 + Tailwind v4, built by electron-vite.
3. **Python backend** (`backend/`) — FastAPI on uvicorn. `engine/transcriber.py` runs the WhisperX pipeline (transcribe → align → diarize). `exporters/video_render.py` renders caption frames with Pillow and muxes with FFmpeg. `models/schemas.py` defines all Pydantic models.

### Dual preload gotcha (read before adding any `window.subforge.*` API)

`package.json` `main` is **`electron/main.js`**, and it loads **`electron/preload.js`** (vanilla CJS) as the preload. `src/preload/index.ts` is a **types mirror**: electron-vite compiles it to `out/preload/`, which nothing loads. Likewise `src/main/index.ts` is a stub that `require`s `electron/main.js` — the TS migration is half-finished, and only `out/renderer/` is consumed from the electron-vite build.

So adding an API means **three** edits: `ipcMain.handle` in `electron/main.js`, the bridge in `electron/preload.js` (runtime), and the typed signature in `src/preload/index.ts` (renderer types). Editing only the TypeScript file type-checks and silently does nothing at runtime.

The newest two are `app:version` → `window.subforge.getVersion()` and `shell:open-external` → `window.subforge.openExternal(url)`, whose URL is allowlisted in the main process by `electron/external-links.js` (https only, a known host, no credentials; `node --test electron/external-links.test.js`). Renderer callers guard both with `typeof window.subforge.getVersion === 'function'`, because a dev app that was not restarted still has the old preload.

### Communication

- **REST** — `backend/main.py` registers ~45 routes; the ones with non-obvious rules:
  - **Local-token gated** (`require_local_token`, 7 routes): `GET /api/fonts/system`, `GET /api/serve-audio`, `GET /api/video-info`, `PUT /api/result`, `POST /api/export`, `POST /api/render-video`, `POST /api/export-hyperframes`. `GET /api/result` is **ungated** — only the PUT is. The library routes (`/api/library/*`) accept **either** token via `require_library_actor`.
  - **Agent-token gated**: all `/api/agent/*` (~19 routes) plus `POST /api/render-frame`.
  - **Deliberately ungated UI mirrors**: `GET|POST /api/coauthor`, `POST /api/coauthor/sync-captions`, and `PUT /api/ui-state` (the write side of the agent-gated `GET /api/agent/ui-state`). The local renderer has no agent token but drives co-author mode from the HyperFrames panel; same loopback trust level as `/api/export-hyperframes`, and the source comments say so. Don't "fix" these by gating them.
- **WebSocket**: `/ws/progress` pushes `ProgressUpdate` events (status + percentage + message). The control socket also carries `record_updated` (a dossier write: `video_id`, `rev`, `by`, read by the Publish soft lock) and `library_changed` (`created[]`, `relinked[]`: the watch folder imported; the library list refetches). The two are deliberately separate, because a watcher is not an actor and must never trip the soft lock.
- **IPC**: renderer ↔ main via `ipcRenderer.invoke()` / `ipcMain.handle()`.
- **Agent commands** (`POST /api/agent/command` → `broadcast_event` → `AgentLiveSync.handleCommand`): `AGENT_COMMAND_OPS` is `set_settings`, `apply_preset`, `set_word_overrides`, `load_video` plus the three caption-track ops `create_track`, `set_track_text`, `reflow_track` and the v3 library's `open_video` (409 without a window or without a stored snapshot; confirmed via `activeVideoId` + the echo). All writes are fire-and-forget commands confirmed by polling the mirror's `agent` echo; the track ops 409 when no window is connected.
- **MCP** (`mcp_server/` → `/api/agent/*`): **61** tools (`grep -c "@mcp.tool()" server.py` reads 34 — the rest register through `TOOLS` tuples: the four caption-track tools in `mcp_server/tracks.py`, the eight v3 library tools `list_videos`, `search_library`, `get_video`, `get_video_transcript`, `set_video_meta`, `mark_published`, `find_video_moments`, `open_video` in `mcp_server/library.py`, the six publish tools in `mcp_server/publish.py` (incl. `grab_frames`), the four collection tools `list_collections`, `get_collection`, `set_collection` (an upsert; `parent_id=""` moves to the top level) and `delete_collection` in `mcp_server/collection_tools.py` (never name a module after a stdlib one: `collections.py` would shadow the stdlib `collections` module whenever the package folder is on `sys.path`), the four channel tools `list_channels`, `get_channel` (the agent's read before drafting for a channel), `set_channel` (an upsert; `primary=True` moves the primary) and `delete_channel` in `mcp_server/channel_tools.py`, and `publish_guide` in `mcp_server/publish_guide.py`, which also registers the `capforge://publish` resources and the four `@mcp.prompt()` slash commands `breakdown`, `describe`, `chapters`, `batch_publish` — the publish workflow as pull-on-demand topics under `mcp_server/publish_guide/`, the MCP-side twin of the bundled `capforge-publish` skill, pinned by `mcp_server/tests/test_publish_guide.py` so every tool a topic or prompt names exists on the server). The four are `create_track`, `set_track_text`, `get_track`, `reflow_track`, and `check_layout` gained a `scan` mode that measures *every* group's wrap instead of rendering one frame (`backend/exporters/layout_scan.py`) — see [docs/caption-tracks.md](docs/caption-tracks.md). The **transcript-editing trio** has the subtle contract: `update_words` takes `WordEdit`s with an `op` field — `replace` (default), `delete`, or `merge` (absorb a word's timing into the adjacent survivor) — and re-joins segment text through an emptiness filter (`mcp_server/cleanup.py`) so deletes/merges never leave a double space. `get_transcript(segments_only=True)` returns a words-stripped shape (`GET /api/agent/result?include_words=false`) to protect the LLM token budget; that route uses `response_model=None` so FastAPI doesn't re-coerce the stripped dict back through `TranscriptionResult`. `sync_captions` is **co-author-mode only** — outside it the backend returns a clear `409`, because `update_words` already mirrors edits to the live UI on its own. See `docs/plans/mcp-transcript-editing-ux.md`.

### Renderer Structure

- `App.tsx` — owns screen state (`library` | `file` | `progress` | `results`; the app starts on `library`), settings, and the right-hand aside (hidden, not unmounted, on the library screen)
- **The `workspace` axis** (`'captions' | 'publish'`, `types/app.ts`) is orthogonal to `Screen`: it switches which aside the *results* screen shows, both stay mounted (`components/publish/PublishAside.tsx`), and it joins the mirror as `UiStateCore.workspace`.
- `components/library/` — the v3 home screen, laid out like a Finder window: LibraryHome (the container: list + first-launch migration + record actions, plus the view prefs, search, the list's channel names, the folder actions and ⌘O → Import…), LibraryScreen (presentational — the files-only capture drop, `LibrarySidebar` beside `LibraryMain`, the open folder menu `LibraryFolderMenu`), `LibrarySidebar` + `LibrarySidebarItem` (fixed `LIBRARY_SIDEBAR_WIDTH_PX` 220: "All videos", a "Folders" label, "Library" with the unfiled count, the folder tree with `total_members` and remembered disclosure, orphan ids with a dashed glyph; `role="tree"`/`treeitem`; "+ New folder" at the foot creates inside the folder on show), `LibraryMain` (masthead: `SidebarToggle`, `LibraryPathBar` — `Library › Events › UCK26`, crumbs navigate and take drops, the last is the `<h1>` — and `LibraryToolbar`, or `SelectionBar` at 2+ selected; the **This folder | All videos** search scope toggle; `LibraryContents` or the empty state; owns the selection via `useLibraryItems` and the selection's right-click `SelectionMenu`), `LibraryContents` (the keyboard scope; Continue hero at All videos/root in grid only, folders first by name, the empty-folder line; what it draws is `visibleContents` in `lib/libraryLocation.ts`), `LibraryToolbar` (`LibraryViewControls` — search, sort + direction, Grid/List, icon-size slider in grid only; hidden on an empty library — **one** `ImportButton` and "Add video"; no collection filter), `LibraryGrid` (folder tiles, then cards, both over `minmax(var(--library-tile),1fr)`, 140–360 px, default 230), `LibraryList` + `LibraryListRow` (table: letterboxed 16:9 thumb, Name, Duration, Status, **Folder** — the full path, only at All videos and in search results — Published on from the summary's `publishedOn`, Modified; folder rows (`FolderRow`) first; header click sorts/reverses; no hero; a row selects like a card), `FolderTile` / `FolderRow` / `FolderGlyph` / `FolderNameInput` and `VideoNameInput` (both over `InlineNameInput`: Enter/blur saves, Esc cancels and never becomes a save, an empty name refused inline) over the per-surface contract `folderItemUi.ts` and the selection contract `libraryItemUi.ts`, `PointMenu` (every right-click menu: fixed, clamped, closed on Esc or a press elsewhere via `hooks/useDismiss.ts`), `FolderMenu` (right-click or `…`: New folder inside… · Rename · Move to… · Folder settings… · Delete, disabled with the reason while it holds videos or subfolders; an orphan gets only "Create folder") and `FolderOptionList` (the folder picker shared with a card's move menu), LibraryCard (the `…` or right-click menu is `LibraryCardMenu` over `hooks/useRecordMenu.ts`, shared with the list row: Rename, Move to folder… (`MoveToCollectionMenu`, the tree indented, "Top level"), Remove, Delete, and "Locate…" for missing media with an inline "link anyway?" confirm; the folder chip shows only where the Folder column would), `LibraryPoster` (poster fetch + the frame's real aspect ratio, measured on load and cached per record, so 9:16 videos get tall cards; the grid is `items-start`), LibraryEmptyState (which embeds DropZoneScreen rather than re-declaring the accepted extensions — `MEDIA_EXTENSIONS` lives in `lib/libraryView.ts`).
  - **Import is one action:** `library:pick-import` returns `[{path, kind}]` and `importPlan` routes folders → import-folder, media → one import-paths batch, `.capforge` → project import, with one combined summary (`lib/libraryImportSummary.ts`). macOS gets a single file-or-folder dialog. Electron cannot combine the two on Windows/Linux, so there the button opens a "Files… / Folder…" menu (`lib/platform.ts`).
  - **The library's drop runs in the capture phase and stops there** and uses the same routing, except that one lone media file still opens in the editor. The embedded DropZoneScreen never sees the drop, which is why it is re-keyed per drop. **It acts only when `dragKind(dataTransfer.types)` is `'files'`** (`lib/libraryDrag.ts`; an internal type wins over `Files` in a mixed list), and so does its drag-over highlight: a card, row or folder being dragged must never reach `droppedImport`.
  - **Folders** ([docs/plans/library-finder.md](docs/plans/library-finder.md) §4; a folder *is* a collection). The screen shows one **location** (`lib/libraryLocation.ts`): `all` (flat), `root` ("Library": top-level folders, unfiled videos, orphan ids as pseudo-folders you can open) or a folder id (direct subfolders + direct videos). It lives in `libraryView.location`; a remembered folder that is gone resolves to the root **at render time and is not written back**, because a prefs write before the stored value is read would win over it. In the main area a folder opens on double-click or Enter; a sidebar folder stays single-click navigation. Search inside a folder is scoped to it and its descendants by default; the toggle resets whenever the location changes.
  - **Drag and drop:** cards/rows drag as `application/x-capforge-videos` (a JSON id array, validated at the drop), folders as `application/x-capforge-collection`. Targets are sidebar folders, "Library", folder tiles/rows, non-last path crumbs and the empty-folder line; `canDrop` withholds the affordance for a no-op or a cycle (`canMoveInto`). `dragover` cannot read the data, so `hooks/useLibraryDrag.ts` remembers the payload from `dragstart`. Videos move through `runMoveVideos` (`lib/collectionMove.ts`: `runMoveToCollection` per video, **one** list + folders refresh, one summary toast); a card's "Move to folder…" uses the same path. A folder moves by `PATCH /collections/{id}` `{parent_id}` (`lib/folderActions.ts`).
  - **Selection** ([docs/plans/library-finder.md](docs/plans/library-finder.md) §4.4–§4.7). **A single click selects; double-click or Enter opens** (a video → `onOpen`, a folder → navigate); the Continue hero stays single-click. Items are keyed `f:<id>` / `v:<id>` in the **visible order** (folders, then the sorted videos; `lib/librarySelection.ts`, a pure reducer: click, ⌘/Ctrl toggle, Shift range from the anchor, ⌘+Shift adds a range, right-click selects an unselected item alone, a drag on a selected video carries every selected **video**). The selection belongs to the location (a new `locationKey` reads as empty) and is pruned to what is visible at read time (`hooks/useLibrarySelection.ts`). The grid is one `listbox` of `option`s, the list a `grid` of rows, both `aria-multiselectable`.
    - **Keys** are handled on `LibraryContents`' container only (`hooks/useLibraryKeyboard.ts` over `lib/libraryKeyboard.ts`), never on `window`, and refused from an input/textarea/select/contenteditable or while a `[aria-modal="true"]` or `[role="menu"]` is **showing** (a hidden one has no client rects). ⌘/Ctrl+A selects every visible **video**; Esc (or a click on empty space) clears; Enter opens the focused item (nothing on another control, nothing on one of several selected); ⌘/Ctrl+⌫ and, off macOS, Delete ask to Remove. Arrows: Left/Right step the visible order; Up/Down step a row in the list or ±columns in the grid, the column count read from a `data-library-grid` section's computed `grid-template-columns` and the index math pure in `lib/libraryNavigation.ts` (folders and videos are two sections; a ragged row clamps to its last item). ⌘O stays `useLibraryImportShortcut`'s.
    - **Bulk actions:** with 2+ selected (or a keyboard Remove to confirm) `SelectionBar` replaces the toolbar: `N selected · Move to… · Remove · Delete… · ✕`; a right-click inside the selection opens `SelectionMenu` with the same three. Remove/Delete apply to videos only and are **disabled with the reason** while a folder is selected; each asks **once**, inline, naming the count, and the confirm is keyed to the selection it counted (`useSelectionCommands`). They run the one-record requests (`removeOne`/`deleteOne` in `useLibraryActions`) through `runBulkRecords` (`lib/libraryBulk.ts`); Move to… runs `runMoveSelection` (`lib/selectionMove.ts`: videos through `moveEachVideo`, the path `runMoveVideos` uses, folders by `PATCH parent_id`, a folder that cannot go there skipped and named). Both: **one** list + folders refresh, **one** failure summary.
    - **Rename in place:** the menu's Rename, or a plain click on the name of the item that already was the one selected, after `RENAME_CLICK_DELAY_MS` (500) — a double-click, a drag, a right-click or another click cancels (`hooks/useRenameClick.ts`). A video writes its root `title` through `runRenameRecord` (`lib/recordRename.ts`: read the rev, `PATCH` with `If-Match`, one retry on a 409, a 422 stays under the input, empty/unchanged sends nothing; transport `hooks/useRecordRename.ts`); a folder goes through the existing folder rename.
  - **View options** ([docs/plans/library-finder.md](docs/plans/library-finder.md) §3): layout, icon size, sort, location, `sidebarCollapsed` and `expanded` are one `app-state` key `libraryView`, parsed per field by `lib/libraryPrefs.ts` (write debounced, flushed on unmount). Search debounces 200 ms, calls `GET /api/library?q=` (`api.listLibrary({q})`), and only *intersects* the returned ids with the search scope (`shownAt`); an older response is dropped (`createLatestOnly`), a failure goes to `notify`, and the hero is hidden while searching. **⌘O is Import…** on the library screen only (the combined picker on macOS, **Files…** on Windows/Linux) and nothing elsewhere; there is no Open.
- `components/TitleBar/` — the window header. There is **no Open button** (a `.capforge` comes in through the library's Import…, or a double-click in Finder); the `project:open` / `dialog:open-projects` IPC is gone. Its **Library** button (file and results screens only, never during `progress`) flushes the pending record autosave (`useAutosave().flush`), then shows the library **without** resetting the session or `activeVideoId`, unlike New. Both handlers live in `hooks/useScreenNavigation.ts` over `lib/screenNavigation.ts`.
- `components/screens/` — DropZoneScreen, ProgressScreen, ResultsScreen, AlignmentNotice, RecoveryBanner
- `components/onboarding/` + `components/tour/` — onboarding, mounted once as `<StartupPrompts screen={screen} />` in App.tsx and reopenable from Settings → General's About block (`requestOnboarding`, the `settingsNavigation.ts` listener-set pattern in `lib/onboardingRequests.ts`; its three kinds are `guide`, `first-video` and `whats-new`). **Two coach-mark tours, not cards** (`lib/tourSteps.ts` `TOURS`): `getting-around` walks the library and opens Settings on Transcription, Channels and Claude for real; `first-video` starts by itself the first time the results screen is shown and walks the editor, player, tracks, style sidebar, export and the Publish workspace, switching the workspace as it goes. A step finds its element by a **`data-tour="<id>"` attribute** — `lib/tourSteps.test.ts` reads every `.tsx` under `components/` from disk and fails when an id here has no attribute there, which is the only thing keeping those attributes alive through a refactor. A step marked `optional` whose element is absent is skipped, any other missing element just centres the card, and leaving `tour.screen` ends the tour quietly. The scrim is **four rectangles around the spotlight, never a mask**, and the overlay root is `pointer-events: none`, so the control being pointed at stays clickable. Pure parts: `lib/tourPlacement.ts` (spotlight box, popover side + flip + clamp), `lib/tourEngine.ts` (stepping, `toursSeen` parsing, whether the editor tour is due), `lib/tourNavigation.ts` (`runTourAction`: Settings through `settingsNavigation.ts`'s open **and** new close channel, the workspace through the one navigator App registers with `useTourNavigator`). `app-state` keys: `toursSeen` (the tours walked) and `lastSeenVersion`. WhatsNewDialog (`lib/releaseNotes.ts`, newest first) is still a card over `components/ui/ModalShell.tsx`; `hooks/useStartupPrompts.ts` reads the evidence and `lib/startupPrompts.ts` rules on it (a fresh install gets the guide, an update gets What's new), and a missing `window.subforge.getVersion` (an old preload) means no prompt, never an error. **Release rule:** `RELEASE_NOTES[0].version` must equal `package.json`'s version and every entry needs a `## CapForge v<version>` heading in CHANGELOG.md — `lib/releaseNotes.test.ts` fails the frontend CI job otherwise, so a release cannot ship without its notes.
- `components/studio/` — StudioPanel (sidebar), StudioCard, PresetPicker, ExportPanel, ExportFooter, CustomRenderPanel, HyperFramesPanel, RenderProgressModal; `studio/sections/` holds the per-card settings UI (Layout, Typography, Colors, Background, Animation, Reading)
- `components/ui/` — shared primitives, incl. **StudioRow** (slider + numeric input + reset; the pattern to copy for any new setting). Its `max` bounds the *drag*; the opt-in `commitMax` bounds what may be *stored*, so a manually typed value is clamped by the backend's `Field(..., le=…)` limit rather than only by `min`. UI writes bypass `sanitizeSettings`, so on a row with a hard schema ceiling that prop is the only guard against storing a config Pydantic will reject at render time. Express it in the row's own units, and omit it where the slider max is merely a recommended range (`posX` past the frame, etc.).
- `components/editor/` — SubtitleEditor (text view), GroupEditor (groups view), WordStylePopup, GroupPositionPopup
- `components/tracks/` — TrackTabs (the language tab strip, mounted in `App.tsx` **above** ResultsScreen because that component remounts per track), LanguagePicker, ReflowBanner
- `components/publish/` — the Publish workspace: WorkspaceToggle (beside TrackTabs), PublishAside (mounts StudioPanel *and* PublishPanel, one `hidden`), PublishPanel (the dossier aside, built from `StudioCard` shells) and its cards (Title, Description, Chapters, Tags, Speakers, Summary, PublishState), PublishFooter (upload package / plain transcript / per-track SRT+VTT), and the per-field furniture ProvenanceChip / FieldHeader / FieldMeter / FieldViolations / AgentUpdateBanner
- `components/player/AudioPlayer.tsx` — video/audio player with WaveSurfer waveform, canvas timeline, caption overlay
- `hooks/` — 58 hooks; the publish ones are `usePublishRecord` (the record + its drafts + the soft lock), `usePublishWriter` (the debounced `PATCH`, the immediate one, and what a 409/422 means) and `usePublishWorkspace` (the toggle, `pendingSeek`, the playhead ref), with `useAgentBridge` holding the five handlers `AgentLiveSync` calls; the library ones are `useLibrarySession` (the record side of a session: `activeVideoId`, `ensureRecordFor`, `openRecord`, `outputDir`, and the autosave writer), `useLibraryList` (refetches on `library_changed`), `useLibraryMigration`, `useLibraryActions` (Remove/Delete, project/folder/multi-file import, Locate; failures go to `notify`, import summaries to `inform` with a tone, since App's `notify` relay always toasts as an error), `useLibraryWatch` (Settings → General's watch folder row), `useLibraryViewPrefs`, `useLibrarySearch`, `useLibraryChannels` (the list's "Published on" names), `useLibraryImportShortcut`, `useRecordMenu` (a card's or row's menu state), the selection ones `useLibraryItems` (composes the rest, builds the item/bar/menu props), `useLibrarySelection`, `useLibraryKeyboard`, `useSelectionCommands`, `useRenameClick`, `useRecordRename` and `useDismiss`, and the folder ones `useLibraryLocation` (navigate, expand, sidebar toggle, create where the user is looking), `useLibraryDrag`, `useFolderMenu` (the open folder menu and the inline rename, screen-wide) and `useFolderActions` (rename/move/delete/adopt/Folder settings…); the load-bearing ones are `useSubtitleOverlay` (Canvas 2D caption preview, must match Pillow), `useTimeline` (zoomable canvas timeline with segment drag + word lane), `useTrackStore` (the caption-track store + its derived active track/classifications/displayGroups), `useTrackActions`, `useRender`, `useTranscription`, `useSettingsUndo`, `useAutosave`
- `lib/` — pure logic, mostly unit-tested (`project.ts`, `renderConstants.ts`, `safeZones.ts`, `cn.ts` are the gaps): `render.ts` (snake_case bridge), `groups.ts` (grouping + reconciliation + gap closing), `api.ts` (REST client + token plumbing), `project.ts` (save/load/restore), `presets.ts`, `fonts.ts`, `wordTiming.ts`, `wordIds.ts`, `endEdited.ts`, `settingsSanitize.ts`, `renderConstants.ts`, `agentCommands.ts`, `undoStack.ts`, `overlayGeometry.ts`, `timelineMath.ts`, `safeZones.ts`, `settingsSearch.ts`, `shortcuts.ts`, and the caption-track core `tracks.ts` (model/create/reflow/mirror entry) + `trackStaleness.ts` + `trackTiming.ts` (bake/link) + `trackChunking.ts` (sibling runs = one inherited caption; translated-track `wordsPerGroup`) + `trackCommands.ts` (the one create/reflow/set-text implementation the UI *and* the agent share) + `uiStateMirror.ts` (the `PUT /api/ui-state` body) + `languages.ts`, and the library core `libraryTypes.ts` (the wire shapes **and** the boundary guard the list is parsed through) + `libraryView.ts` (card derivations + `MEDIA_EXTENSIONS`; the old collection filter helpers are gone) + `libraryLocation.ts` (what All videos / root / a folder contain, breadcrumb, search scope, the gone-folder fallback, `visibleContents`) + `librarySelection.ts` (the selection reducer, `enterTarget`, the rename-click rule) + `libraryNavigation.ts` (arrow-key index math) + `libraryKeyboard.ts` (what a key means, when the contents take it) + `libraryBulk.ts` (bulk Remove/Delete runs, blocker, confirm copy) + `selectionMove.ts` (Move to… for videos and folders) + `recordRename.ts` (the video rename's rev read and single 409 retry) + `librarySidebar.ts` (visible tree rows, expansion) + `libraryDrag.ts` (`dragKind`, payload guards, `canDrop`) + `folderMenu.ts` (disabled reasons, Move to… targets) + `folderActions.ts` (rename/move/delete/adopt runs) + `librarySort.ts` (modified/name/duration/status/created, missing values last both ways) + `libraryPrefs.ts` (the `libraryView` guard) + `librarySearch.ts` (id intersection + stale-response guard) + `librarySession.ts` (where a snapshot goes, the fallback stamp, what clicking a card does) + `libraryOpen.ts` (the agent's `open_video`) + `libraryImport.ts` (what a drop means, the import summary and its toast tone, relink refusals, the watch row's copy) + `libraryApi.ts` (the import/relink/watch REST calls, a sibling of `api.ts` because that file is past its ceiling; it goes through `api.sendWithLocalToken`, so the bridge and token stay in one place), and the publish core `publishTypes.ts` (the dossier/brief wire shapes **and** their boundary guards) + `publishFields.ts` (the field inventory, provenance, revert, the list↔line bridges, the soft-lock merge) + `publishDrafts.ts` (what survives a round trip, what the soft lock protects) + `publishChapters.ts` (insert/remove/rename/suggest) + `youtubeRules.ts` (formatting/snapping/suggesting only) + `exportParams.ts` (the one `POST /api/export` body, shared by ExportPanel and PublishFooter)

**Right-click a word to open WordStylePopup** (text correction + per-word overrides), **right-click a group row to open GroupPositionPopup**. Both are rendered in *both* `ResultsScreen.tsx` and `GroupEditor.tsx` — popup wiring changes must touch both.

### Preview ↔ Render Parity → [docs/caption-parity.md](docs/caption-parity.md)

**Three** caption renderers must produce visually identical output — Canvas preview (`hooks/useSubtitleOverlay.ts`), Pillow (`video_render.py` `_render_frame()`, the source of truth), and the HTML/GSAP layer (`hyperframes_caption_html.py`). **Changing any rendering formula means updating all three in lockstep.**

There are **two layout modes** (`reading_mode`): the default `'wrap'`, and `'rsvp'` — the Spritz speed-reading mode, a single unwrapped line that slides so the active word's focus glyph stays pinned to a pivot column. It is a *layout* axis orthogonal to `word_transition`, so it is **not** a `word_transition`/`wordStyle` value and is not per-word overridable — never add `'rsvp'` to the `WordTransition` union or `WordStylePopup`. RSVP's shared scalar core is triplicated in `backend/exporters/rsvp.py`, `lib/rsvp.ts` and `RSVP_RUNTIME_JS` (`backend/exporters/hyperframes_rsvp_runtime.py`); those three are twins pinned against one another by the shared JSON fixtures under `backend/tests/fixtures/` (`rsvp_*_cases.json`), plus a Canvas↔Pillow numeric fixture asserted from both sides. Pillow's layout/draw lives in `backend/exporters/rsvp_layout.py` (extracted because `video_render.py` is far past the size ceiling).

**RSVP's unit of layout is a *reel*, not a caption group**: consecutive groups that leave no blank frame between them (`a.end >= b.start`, same position override) are merged into one before any renderer sees them, so the line flows through a group boundary instead of snapping back to its first word. Like gap closing this is a group-list transform with **two** implementations, not three — `backend/exporters/rsvp_reels.py` (applied in `groups_for_render`, which also feeds the HTML layer) and `lib/rsvpReels.ts` (applied in `useSubtitleOverlay`) — pinned by `backend/tests/fixtures/rsvp_reel_cases.json`. `gapCloseThreshold` is therefore what decides how long a reel gets; at 0 the pass is the identity. Reels force two mechanisms that must both stay invisible: a **pixel-neutral cull** of the off-window part of the line (Pillow + Canvas only; the HTML layer's mask already does it) and a **per-reel layout cache** (measuring 900 words per frame is 140ms → 9.7ms with both). See [docs/caption-parity.md](docs/caption-parity.md) → "Continuous flow: reels".

Read [docs/caption-parity.md](docs/caption-parity.md) before touching caption geometry, animation, fonts, per-word/per-group overrides, gap closing, or reading mode. It covers the measurement equivalences, the no-bold-synthesis rule, font-loading order, the RSVP formulas (ORP table, `focusOffset`/`lineX`, the caption band, the single anchor rule, the tracking gap, the reticle's half-open box convention), which settings RSVP ignores, the documented accepted deltas (do not "fix" them), and how to run the golden-frame and parity suites.

### HyperFrames Integration → [docs/hyperframes-integration.md](docs/hyperframes-integration.md)

The bridge to the HyperFrames Node CLI subprocess is hardened separately from caption parity: CLI version gate, structured error hierarchy, the durable co-author marker, the scaffold fingerprint cache (**bump `SCAFFOLD_VERSION` when the scaffold or embedded caption runtime changes shape**), the snapshot picker, and the read-only CLI subcommand allowlist.

### Video library (v3.0) → [docs/plans/backend-library.md](docs/plans/backend-library.md)

`backend/library/` is the **backend-owned, durable per-video record** (the "dossier") of the creator-hub plan ([docs/plans/creator-hub-vision.md](docs/plans/creator-hub-vision.md) §2). It bends the "renderer owns, backend mirrors" rule in exactly one place: the *session* (transcript, groups, style, tracks) stays renderer-owned, but `record.json`, the derived `transcript.json`, the assets and a disposable SQLite/FTS5 index live under `$CAPFORGE_HOME/library/<id>/` and are readable with **no window open**. Rules that are easy to break:

- **`backend/library/paths.py` `capforge_home()` is the only reader of `CAPFORGE_HOME`** — discovery (`agent_bridge.discovery_path`), the studio workspace, the import guard and the library all resolve through it (`mcp_server/discovery.py` mirrors the two-line env read because it cannot import `backend`). Don't build `~/.capforge` paths anywhere else.
- **The guard is injected, not imported**: `main.py` calls `build_router(require_library_actor)`; the library package never imports `main.py`, and `backend/library/__init__.py` must stay import-free (`agent_bridge` imports `paths`, so a re-export there would make a cycle). The dependency returns `"agent"` / `"user"` by which token matched, and every write stamps that into the record's capped `history[]`.
- **Every write is `write_json_atomic`** (tmp + `os.replace`); folders are the truth, `library.db` is a cache rebuilt from folders when missing or on a `SCHEMA_VERSION` mismatch, with a `LIKE` fallback when FTS5 is absent and a pure-Python `NullIndex` when `sqlite3` won't import. The title column holds the title **plus the source file's stem as words** (`store_posts.index_title`, re-indexed through `_persist`, so a relink follows the new name); FTS5 terms are quoted **prefix** queries (`"vizu"*`), the two fallbacks substring-match `fold_text` (casefold + diacritics stripped) on both sides. **Bump `SCHEMA_VERSION` when the indexed text or its folding changes**, not only the table shape, or old `library.db` files keep stale rows. The renderer unions the backend's ids with an instant local, diacritic-insensitive match on `displayTitle` (`lib/librarySearch.ts` `searchResults`).
- **`PATCH /api/library/{id}` requires `If-Match: <rev>`** (`428` without it) and a stale rev answers `409` **with the current record in the body** so an agent re-reads instead of clobbering. `RecordPatch` is `extra="forbid"` over the authored fields only; system fields are refused with `422`. A no-op patch does not bump `rev`.
- **Chapters are seconds** (`Chapter.start_s`), never `si-wi` word ids — pinned by `test_library_record_contract.py`, which also partitions every `VideoRecord` field into `AUTHORED_FIELDS` / `SYSTEM_FIELDS` (an unclassified new field fails the backend job, same idea as `test_caption_cfg_contract.py`).
- **Status is derived at read time, never stored**: `imported → transcribed → captioned (a render exists) → drafted (description) → published (publish.youtube.videoId)`.
- **Assets** are served only by the fixed-name allowlist (`poster.jpg`, `peaks.bin`, `thumbnails/<hex>.jpg`) resolved strictly under the record folder; every rejection is the same `404`. The renderer CSP blocks `127.0.0.1` images, so `hooks/usePosterUrl.ts` fetches the card picture (with the local token) into a `blob:` URL: `thumbnails/<cover>` when the list row's derived `cover` is set, else `poster.jpg`. The list is a **summary** (`store._summary`, `SUMMARY_FIELDS` + derived keys) with no `thumbnail`, so anything a card needs from the dossier must be a derived key there, not read off `thumbnail`. Posters are grabbed by `backend/library/posters.py` — one ffmpeg frame a tenth of the way in, written atomically — through the store's `on_created` hook (wired in `router.get_store`, so the agent's `load_video`, project import and the studio migration are all covered) and a bounded startup backfill, both on the module's single-worker pool; `poster` on the record view is derived at read time from the file, like `status`, never stored. The same pool task first probes an unknown `duration` with one ffprobe call (`backend/library/media_probe.py`, `None` on any failure) and stores it via `set_probed_duration` — no `rev` bump, never over a transcript's duration — then grabs the poster at that duration and broadcasts `library_changed` with `updated: [id]`, which the library list refetches on, debounced.
- **Thumbnail frames** (4s part A, [docs/plans/publish-editors.md](docs/plans/publish-editors.md), `backend/library/{frame_grab,frames,store_frames,router_frames,validate_media}.py`):
  - **What a frame is:** a frame grabbed at a time and saved as `thumbnails/<hex>.jpg` inside the record folder. It fits a 1280 box, is never upscaled and stays under 2 MB, so a 9:16 source yields a vertical frame. ffmpeg runs outside `write_lock`.
  - **`POST|DELETE /api/library/{id}/frames` and `POST …/frames/upload` are the only writers of `thumbnail.candidates`.** Each bumps `rev` and announces the change. An upload (a raw JPEG/PNG/WEBP body, normalised by Pillow in `frame_upload.py`) also becomes the cover in the same write; a delete clears `cover` if that frame was the cover.
  - **`PATCH` rules:**
    - A `PATCH` whose `thumbnail` object omits `candidates` (or `cover`) inherits the stored value.
    - An explicitly sent, *different* list is a 422 `candidates_managed`.
    - The renderer always composes `candidates` from the latest record at send time (`lib/publishThumbnail.ts` `composeThumbnailPatch`, applied in `usePublishWriter`). A whole-object thumbnail draft must never carry its own copy, or a frame grabbed meanwhile is dropped.
  - **Saving a frame out:** Electron's `library:save-frame(videoId, name, title)` builds the path itself under the library guard; the renderer never passes a path.
- **Translated metadata (`localized`)** (4s part B, [docs/plans/publish-editors.md](docs/plans/publish-editors.md), `backend/library/{localized,validate_localized,store_localized}.py`):
  - **`PATCH` merges `localized` per language** under the lock: an omitted language is kept, `{lang: null}` removes it, an object replaces that language, and `localized: null` clears all. Before this, a write of `pl` erased `de`.
  - **The renderer's `localized` draft is a *delta*** of the changed languages (`lib/publishLocalized.ts`), composed at send time in `usePublishWriter` beside the thumbnail composition. Never send the whole dict from a draft.
  - **Rules:** every language gets the root hard limits under `localized.<lang>.<field>`. `localized_lang_code` and `localized_is_source` are hard; `localized_chapter_count` is style.
  - **`?lang=` package and validate** render a never-stored localized view: substituted fields, chapter titles by index, source-only title options and highlights dropped, and the fallbacks listed in NOTES. With no `lang`, the output is byte-identical.
  - **`languages` on the single-record view** is derived at read time: the source language, then the stored project's `tracks[].lang`, then the `localized` keys.
- **Copy for platform** (4s part C1, `backend/library/{platform_posts,platform_package}.py`): `GET …/package?platform=linkedin|x|instagram[&lang=]` renders clipboard text only, from the same record, effective brief and `lang` view as the YouTube package; `description` is `null` for these platforms.
  - **LinkedIn:** ≤ 3000 chars, ≤ 5 hashtags, a "Watch:" line.
  - **X:** weighted ≤ 280, where a URL counts 23. Only hashtags are dropped to fit; prose is never cut.
  - **Instagram:** ≤ 2200 chars, "Link in bio", ≤ 30 hashtags.
  - **Findings** go under `package.<platform>`: the over-limit rules are hard, and `video_url_missing` / hashtag counts are style. They are reported, never enforced by truncation.
  - **The YouTube package is byte-identical.**
- **Transcript tab** (4s part C2): `components/editor/TranscriptView.tsx` is the read-only third editor tab (segments, speaker ids, active-row highlight, a chapter gutter via `lib/transcriptChapters.ts`, and "Insert chapter here").
  - **Where its chapters come from:** the Publish controller lives in `App.tsx`, so the tab reads chapters and the insert action through `hooks/usePublishRecordContext.ts` (`PublishRecordProvider` wraps only `ResultsScreen`). Without a provider it has no chapters and no insert.
  - **Rendering:** rows are memoised and receive only `isActive`, so a playback tick re-renders no rows inside a segment.
- **Posts per channel** (multi-channel PR 2, [docs/plans/multi-channel-pr2-contract.md](docs/plans/multi-channel-pr2-contract.md), `backend/library/{record_upgrade,record_projection,record_patching,post_merge,store_patch,store_posts,validate_posts,publish_channels}.py`):
  - **Stored:** `record.json` is `schema: 2`, and a video's publish text lives only in `posts[<channel_id>]` (one `Post` model for every platform). A v1 file is upgraded at read time (`record_upgrade.py`, pure) and backed up once to `record.v1.json` on its first write. Reads never write.
  - **The root fields are the primary channel's post:** `description`, `short_description`, `tags`, `hashtags`, `localized`, `thumbnail.cover` and `publish.youtube` are projected from `posts[primary]` on load and folded back on persist (`record_projection.py`), so every pre-posts reader, today's Publish panel and `set_video_meta` keep working. Any write that changes a post directly goes through `with_post` / `map_posts`, or a stale root overwrites it. Root `title` stays stored (the library name); a root title write is bridged into the primary post.
  - **`PATCH` `posts` merges per channel and per field** under the lock (`{id: null}` removes, `hidden: true` hides and keeps the text); an unknown channel is `unknown_channel`, and a projected field sent both at the root and under the primary post is `ambiguous_post_field`.
  - **Rules:** a YouTube post is judged by today's validators over `record_for_channel`, readdressed to `posts.<id>.<field>`; other platforms by the `platforms.py` limits on the pasted text (body + hashtags line), plus `field_not_on_platform`.
  - **`?channel=`** on `GET …/package` and `POST /validate`; no channel is the primary, byte-identical. Status, search text and the summary's derived `publishedOn` read every visible post. A channel with posts cannot be deleted (`channel_in_use`), and `GET /channels/{id}?include_recent_posts=true` is opt-in only.
- **Publish tabs** (multi-channel PR 3, [docs/plans/multi-channel-pr3-contract.md](docs/plans/multi-channel-pr3-contract.md), `components/publish/{ChannelTabs,ChannelSection,PublishToChecklist,PostTextCard,PostCoverPicker,PostPublishedCard,ChannelLanguageChip,PublishVideoSection,KeywordsCard}.tsx`, `hooks/usePublishChannels.ts`, `lib/{publishPosts,channelPublishView,platformSpecs,postsApi,publishWire}.ts`):
  - **The one write rule:** every field on a tab writes `posts.<channel_id>.<field>`; the **only** exception is the Title on the primary channel's tab, which writes the root `title` (the library name the backend bridges into that post). So the UI can never send a projected root field and the same field under `posts` — a 422 `ambiguous_post_field`. `lib/publishWire.ts` is the one place a write becomes its wire body, and it strips the root `thumbnail.cover` from **every** patch, because covers are per channel.
  - **`channelPublishController`** (`lib/channelPublishView.ts`) hands each tab a `PublishController` whose record *is* that channel's post, which is why the existing Title/Description/Tags/Localized/PublishState cards work per channel unchanged. Provenance and revert on a tab read the `posts.<id>` history entry, so the chip is per post, not per field.
  - **`posts` is deliberately not in `PUBLISH_FIELDS`**: that inventory feeds `authoredFields`, the body of the *root* validator. A tab is validated by `POST /validate` with `channel`.
  - **The mirror** carries `activeChannelId`; `PUT /api/ui-state` takes a free-form dict, so no backend model changed.
  - The footer copies the **active tab's** package (`getChannelPackage`); the old platform dropdown is gone. `lib/platformSpecs.ts` meters counts only (chars/bytes/utf16/weighted/items) — every ruling still comes from the backend.
- **Import channels, "Start from…" and the end of `platform=`** (multi-channel PR 4, [docs/plans/multi-channel-pr4-contract.md](docs/plans/multi-channel-pr4-contract.md), `backend/library/post_drafts.py`, `lib/{importChannels,publishStartFrom}.ts`, `hooks/useImportChannels.tsx`, `components/library/PublishToSheet.tsx`, `components/publish/{ChannelChecklist,StartFromStrip}.tsx`):
  - **`POST /{video_id}/posts/{channel_id}/draft?from=<channel>`** renders one tab's text as another tab's draft and **never stores it** (no write, no `rev`, no history; findings are not returned — `POST /validate` judges it once it lands). A platform with a layout goes through `platform_posts.render_platform_post` and its trailing hashtag line is split off; TikTok has no layout, so it takes the source body as it is; a YouTube target is never packaged. The target's `profile.default_hashtags` stay out of **both** halves, because `pasted_text` re-adds them at copy time. `post_drafts.py` takes `read_collection` **injected** — importing `router_publish` back would close a cycle.
  - **`platform=` is gone** from `GET …/package` and from MCP `get_upload_package`; `platform_package.py` was deleted with `PACKAGE_PLATFORMS` / `unsupported_platform_detail`, while **`platform_posts.py` stays** as the draft renderer. The YouTube package is byte-identical.
  - **The import "Publish to:" ask happens in two places, never one:** batch imports (drop, Import…) go through `useLibraryActions.runImport` before the first request, while "Add video" and a single dropped file have no record until Start, so the ids are passed **into** `ensureRecordFor`. Never ask inside `ensureRecordFor`: the agent's `load_video` and a project restore both call it and must stay channel-less.
  - **An empty choice sends no `channels` key at all** (`channelsBody`), so an import with nothing ticked is byte-identical to the pre-PR request. `POST /import-project` gained `channels` too, or a mixed batch would half-apply the choice. The remembered choice is the `app-state` key `lastPublishChannels` (`electron/app-state.js`), filtered at read time against the channels Settings still has.
- **Scratch records** (`scratch: true`) hide under `.scratch/`, are read-only until `POST …/promote`, and are pruned after `SCRATCH_LIFESPAN_DAYS` at startup — agent QA runs must not pollute the library.
- **Folder import, relink and the watch folder** (3s, [docs/plans/library-folder-import.md](docs/plans/library-folder-import.md), `backend/library/{media_scan,folder_import,watch,router_import}.py`):
  - **A fingerprint hit on a record whose media is missing is a relink**, not a skip, so importing the folder a drive was moved to heals its cards.
  - **`sourcePath` stays a system field**: it changes only through `POST …/{id}/relink`, never `PATCH`. Relink rewrites the stored `project.capforge` / `transcript.json` media paths too, because restore opens the snapshot's `audioPath`, not the record's. Different media needs `force`; a file another record owns is refused even with `force`.
  - **The watcher polls** (10 s, only while CapForge runs), imports a file only once its size+mtime hold still for two polls, and **remembers every path it took in `watch.json` `seen`**. Fingerprint lookup ignores `.removed/`/`.trash/`, so without `seen` a Remove would be undone on the next tick. Its scan and import run outside its state lock, so `GET|PUT /watch` never wait on an import pass.
  - **`LibraryStore` writes are serialised by one re-entrant lock**, and `LibraryIndex` has its own lock over its single shared sqlite connection. The watcher thread writes concurrently with FastAPI's threadpool, and every store write is read-modify-write. Take the store lock in any new mutating method.
  - **Media extensions have three copies**: `media_scan.py`, `lib/libraryView.ts`, and `electron/single-instance.js` (which also feeds the Locate picker's filter through `electron/library-dialogs.js`). All three are pinned by `backend/tests/fixtures/media_extensions.json`; change one, change all three.
- **Collections and description template slots** (4s part 1, [docs/plans/library-collections.md](docs/plans/library-collections.md), `backend/library/{collection_store,template,router_collections}.py`):
  - **A collection** is an entity in `collections.json` (slug `id`, `name`, `slots`, per-field `overrides`, no `If-Match`). Membership stays the record's authored `collection_id`, and a `PATCH` naming no collection is a 422 `unknown_collection`. Records with a dangling id are listed as `orphans` so a collection can adopt them by that exact id. Delete refuses a collection with members.
  - **The effective brief** is the channel brief with the collection's overrides applied: `null` inherits, lists and blocks replace, and `slots` merge key-wise.
  - **Collections nest** ([docs/plans/library-finder.md](docs/plans/library-finder.md) §2, `backend/library/collection_tree.py`, `lib/collectionTree.ts`). The UI says **Folder** everywhere a person reads it (the library, the Publish card, Settings → Folders); code, routes, MCP and `collection_id` keep "collection". The renderer's `CollectionSummary` carries `parent_id`, `total_members` and `path` (an older backend parses as top level).
    - **"Folder settings…"** calls `requestSettingsCategory('collections', { collectionId })`; `SettingsDialog` remounts the Folders pane with `initialSelectedId`.
    - **Stored:** `parent_id` (`null` = top level). A top-level row is written *without* the key, because an older build's `Collection` forbids unknown keys; a file that really nests is unreadable to one.
    - **Refusals, checked under the store lock:** 422 `unknown_parent`, `collection_cycle`, `collection_too_deep` (`MAX_COLLECTION_DEPTH` 8; a top-level folder is depth 1, and a move counts its whole subtree). Delete answers `collection_in_use` first, then 409 `collection_has_children` with `children`. A dangling or looping `parent_id` in the file is `CollectionsUnreadable`.
    - **The one seam:** `resolved_collection` folds the ancestor chain root first (slots key-wise, each override the deepest non-null, identity the leaf's). `router_publish.read_collection` (also injected into `post_drafts.py`), `_requested_collection` and `router_collections._detail` go through it; `effective_brief` and its seven callers are unchanged. A top-level collection resolves to itself, byte-identical. A detail's own `slots`/`overrides` stay unresolved, since they are what the editor edits.
    - **Routes:** `GET /collections` stays flat; every collection answer adds `parent_id`, `path` (names, top level first) and `total_members`. `GET /api/library?collection=` is still direct members only.
  - **The upload package is rendered at read time** from the record plus the effective brief. That *is* "regenerate an event's footers in one pass": a collection edit changes every member's package with no record write, so never add a bulk per-video rewrite for it.
  - **`Brief.description_template`:** empty means today's layout, **byte-identical**, and the existing package tests prove it. Never edit their expectations to make a template change pass.
  - **The built-in slots** are pinned by `backend/tests/fixtures/builtin_slots.json` against `template.py` and `lib/collections.ts`. Custom slots may not shadow them.
  - **Unfilled slots and size:** an unknown `{{slot}}` is printed as written and reported as `package.description` / `unknown_slot`, never silently dropped. The *assembled* description is also held to YouTube's 5000 bytes and no angle brackets, because the record's own `description` field is only part of the pasted text.
  - **The package response carries `description`** (the pasteable body) beside `text`; the renderer's preview must read it, never scrape `text`.
- **Channels** (multi-channel PR 1, [docs/plans/multi-channel-pr1-contract.md](docs/plans/multi-channel-pr1-contract.md), `backend/library/{channels,channel_store,router_channels,platforms}.py`):
  - **`channels.json`** (library root, `@writes` under the store lock) holds each channel's `context` (what the agent reads before drafting) and `profile` (the brief's boilerplate the package pastes). A missing file is **bootstrapped once from `brief.json`**, which is left untouched and is never written again.
  - **The brief is a view of the primary channel**: `router_publish.read_brief` returns `brief_from_channel(primary)`, and `PATCH /brief` patches the primary. That one seam keeps the package, validators, platform posts and collections byte-identical (pinned by `test_library_channels_byte_identity.py`). The **primary is always YouTube**: a non-YouTube primary is a 422 `primary_not_youtube`, and deleting it is a 409 `channel_is_primary`.
  - **`platforms.py` is the one home of platform limits** (`GET /api/library/platforms`). YouTube's import from `validate.py`, and `platform_posts.py` imports its numbers back. Never restate one.
- Nothing library-related may ever become a `VideoRenderConfig` field; `record_id` will ride the **request** models.

### TypeScript Config

Three tsconfig files: `tsconfig.json` (root references), `tsconfig.node.json` (main + preload), `tsconfig.web.json` (renderer). Type-check the renderer with `npm run typecheck`. Path alias `@/*` → `src/renderer/src/*`.

## Key Conventions

- **snake_case ↔ camelCase bridge**: the backend is snake_case, the frontend camelCase, and the bridge happens in exactly one place — `lib/render.ts` (`buildRenderBody()`). Adding a setting is a **seven-file** change, and skipping any of the last three fails CI or ships a dead control:
  1. `StudioSettings` interface + `DEFAULTS` (`StudioPanel.tsx`)
  2. `render.ts` config object
  3. `VideoRenderConfig` Pydantic model (`schemas.py`)
  4. a `StudioRow` in the right card under `components/studio/sections/` — otherwise there is no UI
  5. `lib/settingsSanitize.ts` — bounds mirroring the Pydantic `Field(...)`
  6. `lib/settingsSearch.ts` — both `CARD_SETTINGS` and `SETTINGS_REGISTRY`, or settings search can't find it
  7. `backend/tests/test_caption_cfg_contract.py` — an always-on partition test: every `VideoRenderConfig` field must be filed in exactly one of `EXPECTED_IN_CAP_CFG` or `INTENTIONALLY_ABSENT`, so an unclassified new field **fails the backend CI job**. It guards the bug class where `caption_cfg` silently dropped style fields.
- **StudioSettings**: one flat interface in `StudioPanel.tsx` holding every caption style setting; passed down as props, never fragmented. Defaults are declared as `const DEFAULTS` and re-exported as `STUDIO_DEFAULTS` — grepping for the latter inside `StudioPanel.tsx` only finds the alias line.
- **StudioSettings units are NOT uniform, and external writers must be sanitized (`lib/settingsSanitize.ts`)**:
  - **0–100, divided by `pct()` in `render.ts`**: `bgOpacity`, `maxWidth`, `posX`, `posY`, `animDuration`, `rsvpPivotX`, `rsvpEdgeFade` (the last two are fractions of the RSVP caption band; their backend `Field` bounds are the /100 versions, `le=1.0` and `le=0.5`).
  - **0–1 fractions, pass through untouched**: `shadowOpacity`, `highlightOpacity`, `rsvpContextOpacity` — the only three marked `fraction: true`.
  - **Plain seconds**: `gapCloseThreshold`, `lastGroupHold`, `rsvpSlideDuration`. Marking these `fraction: true` would re-read a legitimate `2` second hold as `0.02`, or a `1.5`s RSVP slide as `0.015`s.
  - **Unbounded above**: `bounceStrength` is a fraction *of font size* with no upper bound (`Field(0.18, ge=0.0)`), so a value > 1 is legal. It is deliberately **not** `fraction: true` — "fixing" that would corrupt a legitimate `1.5`.
  - **Non-numeric**: `readingMode` (`'wrap' | 'rsvp'` enum), `rsvpFocusColor` (hex, default `#E4851F`), `rsvpReticle` (bool).
  The UI sliders can't get this wrong, but the three writers that bypass them can — the MCP agent (`set_settings`), a preset (`applyPreset`), and a restored project (`restoreFromProjectFile`) — so all three run values through `sanitizeSettings`/`sanitizeSettingValue`, whose bounds mirror the `Field(...)` constraints on `VideoRenderConfig`. A `fraction` field given a value in (1, 100] is read as a percentage (90 → 0.9) rather than clamped to 1.0, because that is the mistake being made. Without this, an agent writing `shadowOpacity: 90` poisons the mirrored config and *every* later render fails validation (422 on `/api/render-video`, 409 on `/api/render-frame`) — and saving that style as a preset makes it permanent, so "change a control to re-mirror" never clears it.
  `settingsSanitize.ts` also has a second, **non-numeric** path (enum / hex colour / bool) for the same three bypass writers. Only the three RSVP non-numeric settings are enrolled (`ENUM_SETTING_VALUES`, `COLOR_SETTINGS`, `BOOLEAN_SETTINGS`); the pre-existing enum/colour settings (`textAlignH`, `wordStyle`, `textColor`, …) are deliberately **not**, because enrolling them would change what an agent's `set_settings` is allowed to write — that belongs in its own change. Note the two readings differ on purpose: a live patch drops an unusable value and leaves the user where they are (`coerceNonNumericSettingValue`), while a preset/project restore substitutes the default.
- **Settings undo**: `useSettingsUndo` (App.tsx) wraps `setSettings`; every UI change is pushed to a ref-based stack. The mechanics — `MAX_HISTORY = 50`, 500ms debounce — live in `lib/undoStack.ts`. Cmd+Z / Cmd+Shift+Z when focus is outside text editors.
- **Word-timing locality (`lib/wordTiming.ts`)**: a manual text correction must never move a word the user didn't touch. CapForge is a *finishing* tool used after the video is cut elsewhere, so captions stay locked to the original audio — the invariant `mcp_server/cleanup.py` states as "Timing is never shifted". `retimeWords()` is the **single** place text→timing reconciliation happens: it LCS-diffs the old word text against the new tokens, so matched words keep byte-identical `start`/`end` plus their `overrides`, and only the changed run is retimed **inside its own span**. An inserted word takes the silent gap between its neighbours when one exists, else carves off the tail of the word before it (at most one neighbour moves); a deleted word's span is absorbed by the previous survivor, mirroring `apply_word_edits()`. **Never re-time words by array index** — that was the original bug (inserting one word shifted every later word onto its neighbour's slot and duplicated the last timing). `/api/realign` (WhisperX forced alignment) rewrites *every* word in a segment and is deliberately a manual button only — never auto-trigger it from an edit handler.
- **Segments vs Groups**: `Segment[]` is the source transcription. Groups are derived display chunks (N words each) used for preview and render; they can be manually edited (merge/split/reorder), tracked by the `groupsEdited` flag. Two group-only fields persist with the project (they ride `studioGroups`) but are intentionally **not** part of presets:
  - `positionOverride` (`position_x`/`position_y`) — set by right-clicking a group row. Position-only changes are **not** boundary edits: they flow through GroupEditor's `onPositionChange` (not `onChange`) and must NEVER flip `groupsEdited`, so automatic re-grouping keeps working. `render.ts` still sends `custom_groups` whenever any group has an override.
  - `endEdited` — "the user placed this `end` by hand", so automatic gap closing and the final-group hold skip it (`lib/endEdited.ts` retrofits it onto pre-`endEdited` projects on restore). Unlike `positionOverride`, an end edit **is** a boundary edit: it flows through `onChange` and does flip `groupsEdited`, including the `↺` reset that hands the group back to the automatic pass.
- **Group membership is reconciled by word identity, never by position (`lib/wordIds.ts`)**: every word carries a session-minted `wid`, and the one place segments flow back into manually-edited groups is `reconcileGroups()` (`lib/groups.ts`), called from `ResultsScreen`'s sync effect. It matches group words to segment words by `wid`, so a word dragged into a non-adjacent group, a reordered group, a merge or a split all survive an edit to the source segments; a group whose word ids are unchanged also keeps its `start`/`end` verbatim, which preserves both a manual timeline drag and the end baked by "Close all gaps". **Never re-slice `segments.flatMap(s => s.words)` by index or word count to refresh groups** — that silently restores document order and is exactly the bug this replaced (`docs/plans/fill-gaps-resets-custom-groups.md`). Ids are minted at every segments write via `commitSegments` → `ensureWordIds`, carried across `retimeWords` and `/api/realign`, adopted onto legacy project groups by text+timing (`adoptWordIds`), and stripped from the `custom_groups` render payload in `render.ts`. Identity is all-or-nothing: if any word lacks a `wid`, `reconcileGroups` falls back to a document-order rebuild rather than half-matching.
- **Caption tracks (multi-language tabs) → [docs/caption-tracks.md](docs/caption-tracks.md)**: a track is a named bundle of exactly `buildRenderBody()`'s three inputs (settings + groups + `groupsEdited`), which is why **no caption renderer changed** and why nothing track-related may ever become a `VideoRenderConfig` field. **The renderer owns tracks; the backend only mirrors them; agents command and confirm** — there is no canonical track state in Python and **no Python twin** of bake/link/staleness/reflow (unlike gap closing's two and RSVP's three). The mirror (`PUT /api/ui-state`, composed by `lib/uiStateMirror.ts`) keeps its seven existing keys describing the *active* track and adds `tracks[]` (compact groups, **no words**), `activeTrackId`, `activeVideoId` (v3 library: the record `open_video` last installed, `null` otherwise), and the `agent` command echo the MCP tools poll. Four optional fields carry it: `Segment.sourceWords` / `timingLinked` / `previousText` are **group-only and translated-track-only**, `Word.timingDerived` marks a word whose timing came from the group span. Consequences worth knowing before you touch any of it:
  - A translated track is `autoGroup: false` — its grouping is inherited and it must **never** reach the `buildStudioGroups` rebuild branch. *Words per group* still applies there, meaning something else: it re-chunks each translated **sentence** (`lib/trackChunking.ts`), never merging across a sentence boundary. The link (`propagateSourceTiming`) and the reflow carry-over both read **sibling runs** — consecutive groups whose `sourceWords` wid lists are identical — not groups; staleness deliberately still reads groups, so every chunk gets its own chip.
  - **A translated track has the same two levels the source has: sentences and captions** (`lib/trackSentences.ts`). A group's sentence is the source *segment* its **first** recorded `sourceWords` wid lives in (`widToSegment`, derived once in `useTrackStore` from the source's segments). `track.segments` on a translated track is not authored — it is **always** `sentenceSegmentsFor(track.groups, widToSegment)`, written in exactly one place (`withSentenceSegments`, `lib/tracks.ts`) and called from every seam that writes translated groups. Creating a track 1:1 with source *groups* was the QA bug: the Text view listed 41 caption fragments against the source's 12 sentences, the agent translated fragments in isolation, and *Words/Grp* could only cut inside one.
  - **Agent transcript edits (`update_words`, `remove_filler_words`) target the source track regardless of which tab is active** — never apply `result_updated` to whichever `ResultsScreen` happens to be mounted.
  - Staleness compares `{wid, text}` **pairs**, not wid sets: a one-for-one typo fix deliberately keeps its `wid` (`wordTiming.ts`), so a wid-only compare would never notice a corrected word. "Reflowed" is one boolean per track, not a per-group state.
  - `custom_groups: []` ≠ `None`. An all-placeholder translated track mirrors `[]`, so `groups_for_render` tests `custom_groups is not None`; truthiness would draw the *source* language over the translated track.
  - `output_name_suffix` lives on the **request** models (`VideoRenderRequest`, `HyperframesRenderRequest`) and on the mirror's render **body**, never inside `VideoRenderConfig`.
  - Project file `version: 2` is purely additive — every v1 key keeps its v1 meaning and describes the source track; older builds ignore `tracks` and open the source.
- **Bundled skills (`mcp_server/skills/<name>/SKILL.md`)**: workflows Claude runs against CapForge. The app lists them from that folder (frontmatter `name` must equal the folder name — pinned by `mcp_server/tests/test_bundled_skills.py`, which also checks every tool a skill names exists), seeds a per-user copy at `~/.capforge/skills/<name>/` on first open, edits *that* copy in Settings → Skills, and installs it into `~/.claude/skills/<name>/` for Claude Code (`electron/skills-store.js`). A bundled change is flagged against the stored `.bundled-sha1`, never applied over user edits. Adding a skill is one folder plus, if it needs tools the server must keep, an entry in that test's `REQUIRED_TOOLS`.
- **The library record is the primary owner of durable state; `autosave.json` is only the fallback**: a session belongs to a library record (`activeVideoId`), and the 2 s autosave PUTs the project snapshot into it (`PUT /api/library/{id}/project`). The local `autosave.json` is written **only** when that PUT fails, stamped with `recordId` + `rev` so the local copy is recognisable as a fallback for a known record, and the user is toasted once per session. `activeVideoId` is set by Start (`ensureRecordFor` → `POST /api/library`, create-on-drop), by the agent's `load_video` and `open_video`, and by a project restore; it is cleared by **New**, which returns to the library screen. All of it lives in `hooks/useLibrarySession.ts` over the pure `lib/librarySession.ts` — a failed record write never stops a session, it only downgrades it to the local copy.
- **Backend port**: preferred 53421; `python-manager.js` finds a free port and the renderer gets it via IPC `backend:port`.
- **Custom fonts**: stored in app data via Electron IPC. The path goes to both Canvas (via `@font-face` injection) and the backend (`custom_font_path`). Bold means selecting a bold font variant — there is no synthetic bold.
- **Shareable presets (`.cfpreset`)**: a preset exports to / imports from a single JSON file (`{ type: 'capforge-preset', version, name, settings, font }`) via IPC `presets:export` / `presets:import` (preload: `window.subforge.exportPreset(name)` / `importPreset()`). `electron/preset-io.js` is the source of truth for the format and its pure helpers (`classifyFont`, `buildPresetExport`, `parsePresetImport`, `uniquePresetName`). Font portability: user fonts are embedded as base64 (10MB cap), bundled CapForge fonts are referenced by name only; on import the font is re-materialized to a *local* path and the stored `customFontPath` is rewritten so Canvas and the backend both resolve it. Import is a **trust boundary** — `parsePresetImport` validates the type tag, gates the version, strips proto-pollution keys, enforces the size cap, and writes fonts basename-only with an extension allowlist. Per-word `custom_font_path` overrides live in project data, not presets, so they are intentionally not shared.
- **Canvas wheel events**: React's `onWheel` is passive and cannot `preventDefault()`. For canvas zoom/pan use a native `addEventListener('wheel', handler, { passive: false })` inside a `useEffect` that cleans up on unmount. Always call `draw()` after mutating state refs — a React re-render doesn't repaint a canvas.
- **Toasts**: `useToast` provides `toast(message, type)`. Wrap errors in toast calls rather than silently catching.
- **Local media token**: the 7 gated routes listed under Communication stream/repoint arbitrary local files or trigger a render/export to a client-supplied `output_dir`, so all carry a per-launch `CAPFORGE_LOCAL_TOKEN`. Electron mints it (`crypto.randomBytes(32)`) per spawn, injects it as an env var, and exposes it via IPC `backend:local-token` → `window.subforge.getLocalToken()` (wired in **both** preloads — see the Dual preload gotcha). The renderer calls `api.setLocalToken()` alongside every `setPort`, sending it as `?token=` on `<audio>`/WaveSurfer URLs (which can't set headers) and as the `X-CapForge-Local-Token` header on `fetch`. That header is attached centrally in `api.ts`'s `post()`, so a newly gated POST route gets it for free — don't hand-wire it. `require_local_token` (`main.py`) compares constant-time (`hmac.compare_digest` via `token_matches`) and also accepts `X-CapForge-Agent-Token` — the header the MCP client (`mcp_server/client.py`) sends on every call — so an authorised agent isn't locked out of the gated render/export routes. A second guard, `_is_servable_path`, realpath-resolves the target and serves only `current_result.audio_path` or a file *contained in* its `hyperframes_workspace()` — defeating `../`, symlink, and sibling-prefix traversal even for a token holder. The export/render routes additionally sandbox `output_dir` through `resolve_output_dir()` (`hyperframes_project.py`), so a non-absolute value (a `..` string, or the schema default `"output"`) falls back to the folder next to the source media instead of being honoured literally. uvicorn runs `--no-access-log` so the query-param token never lands in `backend.log`. Never hardcode, persist, or log this token.
- **Overlay MOV alpha convention**: the transparent overlay export writes **premultiplied** alpha in the ProRes 4444 branch of `_render_overlay` (`-vf premultiply=inplace=1`), because ProRes 4444/QuickTime is a premultiplied-alpha convention — straight alpha risks mis-compositing in NLEs. (Premiere may still auto-detect the clip as "Straight Alpha"; users should conform it to Premultiplied.) The WebM overlay branch stays **straight** alpha (VP9/browser convention) — do not touch it. Pinned by `backend/tests/test_overlay_alpha.py`. To simulate an NLE import: `[1:v]unpremultiply=inplace=1[u];[0:v][u]overlay`. **Known non-file failure mode**: a washed-out semi-transparent caption box in Premiere with a *correct* file is Premiere compositing in linear color (sequence "Composite in Linear Color") — verify with a gamma-space ffmpeg composite before touching the encoder (`docs/plans/overlay-opacity-persists-round2.md`).
- **BT.709 color tagging**: every user-facing encode except WebM (overlay MOV, overlay MP4, baked MP4) forces the RGB→YUV conversion to BT.709 limited range (`scale=out_color_matrix=bt709:out_range=tv,format=…`) **and** tags the stream (`_BT709_TAGS`), so NLEs stop guessing. Matrix and tags must always change together — one without the other reintroduces a hue shift on saturated colors (pinned by the saturated-color test in `test_overlay_alpha.py`). In the MOV branch `premultiply` must stay **first** in the `-vf` chain (it operates in RGBA), and the branch also needs `-movflags write_colr` — without it the mov muxer writes no `colr` atom for ProRes at all. Only `color_space` is ffprobe-visible there; re-verify on any FFmpeg upgrade.
- **Publish validators live in Python, once**: every rule about a dossier field — YouTube's hard limits (title ≤ 100 chars, description ≤ 5000 **bytes**, tags ≤ 500 chars, no angle brackets, chapters starting at `00:00`, ≥ 3, ascending, ≥ 10 s apart, inside the duration) and the brief's house style — lives in `backend/library/validate.py` and is reached through `POST /api/library/validate` (unsaved panel text included) and a `422 {violations}` from `PATCH /api/library/{id}`. The renderer **meters, snaps and formats** and nothing else: `lib/youtubeRules.ts` re-declares the limits only so a counter can be drawn without a round trip, `snapToWordStart` needs the live words, and `chapterSuggestions` is a suggestion, not a ruling. Never add a second copy of a rule here — render the backend's `violations` under the field instead (`components/publish/FieldViolations.tsx`).
- **`formatTimestamp` is the one publish twin**: `lib/youtubeRules.ts` and `backend/library/package.py`'s `format_timestamp` must produce byte-identical `MM:SS` / `H:MM:SS`, because the chapter rows on screen and the chapter lines in the copied upload package are the same timestamps. Pinned from both sides by the shared fixture `backend/tests/fixtures/timestamp_cases.json` (the RSVP precedent) — change one, change both, and extend the fixture.

## Theming

Dark (default) and light themes via the `:root.light` CSS class. All colors are CSS custom properties in `src/renderer/src/styles/globals.css` (`--color-text`, `--color-bg`, `--color-surface`, …).

- Never hardcode colors like `text-white` or `bg-black` — use `var(--color-text)`, `var(--color-bg)`, etc.
- Tailwind v4 can misparse `text-[var(--color-text)]` (ambiguous color vs font-size) — use inline `style={{ color: 'var(--color-text)' }}` when it does.
- Font variables: `--cf-font-ui` (Inter), `--cf-font-display` (Instrument Serif), `--cf-font-mono` (JetBrains Mono).
- Brand orange is the `--color-brand` token (`#D4952A`).
